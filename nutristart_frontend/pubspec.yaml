name: nutristart_frontend

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.5.1
  dio: ^5.4.0
  go_router: ^14.0.0
  # flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.5.3

flutter:
  uses-material-design: true